{% extends 'base.html.twig' %}
{% block title %}Edit module {% endblock %}

{% block body %}
    <div class="container my-5">
        <h1 class="text-center">Edit module</h1>

        {{ form_start(formEditModule) }}
            <div class="row">
                <div class="col-6">
                    {{ form_row(formEditModule.name) }}
                </div>
            </div>
            <div class="row">
                <div class="col-6">
                    {{ form_row(formEditModule.category) }}
                </div>
            </div>
            
            {{ form_row(formEditModule.Submit) }}
        {{ form_end(formEditModule) }}
    </div>
    
{% endblock body %}